#ifndef TYPES_HPP
#define TYPES_HPP

struct Position {
    int x;
    int y;
};

struct Size {
    int width;
    int height;
};

struct Color {
    Uint8 r;
    Uint8 g;
    Uint8 b;
    Uint8 a;
};

#endif // TYPES_HPP
